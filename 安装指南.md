# 📦 PDF 试题模拟考试器 - 安装指南

## 🎯 系统要求

- **Python 3.8+** （推荐 3.9 或更高版本）
- **操作系统**: Windows / macOS / Linux
- **网络连接**: AI 解析需要联网

## 🚀 快速安装（3步）

### 步骤 1: 克隆或下载项目

```bash
# 方法 1: 使用 Git 克隆
git clone <repository-url>
cd Exam

# 方法 2: 直接下载
# 下载 ZIP 文件并解压
```

### 步骤 2: 安装依赖

```bash
# 使用 pip 安装所有依赖
pip install -r requirements.txt
```

**依赖包说明：**
- `PyPDF2` - PDF 文本提取
- `openai` - DeepSeek API 调用
- `Flask` - Web 服务器（仅网页版需要）
- `flask-cors` - 跨域支持（仅网页版需要）

### 步骤 3: 配置 API Key（可选）

如果要使用 AI 解析功能，需要配置 DeepSeek API Key：

#### Windows (PowerShell)
```powershell
$env:DEEPSEEK_API_KEY = "your-api-key-here"
```

#### Linux / macOS
```bash
export DEEPSEEK_API_KEY="your-api-key-here"
```

#### 或者直接修改 utils.py
```python
# 在 utils.py 中找到这一行：
api_key = os.environ.get("DEEPSEEK_API_KEY", "默认key")

# 改为：
api_key = "your-api-key-here"
```

## ✅ 验证安装

运行以下命令验证所有依赖是否正确安装：

```bash
python -c "import PyPDF2; from utils import call_llm; print('✅ 安装成功！')"
```

**预期输出：**
```
⚠️ 警告：正在使用默认的 API Key，建议设置环境变量 DEEPSEEK_API_KEY
✅ 安装成功！
```

## 🎮 运行程序

### 桌面版（推荐）⭐

```bash
python exam_app.py
```

### 网页版

```bash
# 1. 启动服务器
python server.py

# 2. 在浏览器中打开 Exam.html
```

## 🐛 常见问题

### 问题 1: "No module named 'PyPDF2'"

**原因**: PyPDF2 未安装

**解决方法**:
```bash
pip install PyPDF2
```

### 问题 2: "No module named 'tkinter'"

**原因**: Tkinter 未安装（Linux 用户常见）

**解决方法**:

**Ubuntu/Debian:**
```bash
sudo apt-get install python3-tk
```

**Fedora:**
```bash
sudo dnf install python3-tkinter
```

**macOS:**
```bash
# Tkinter 通常已包含在 Python 中
# 如果没有，重新安装 Python
brew install python-tk
```

**Windows:**
- Tkinter 通常随 Python 一起安装
- 如果没有，重新运行 Python 安装程序并勾选 "tcl/tk" 选项

### 问题 3: "ModuleNotFoundError: No module named 'openai'"

**原因**: OpenAI SDK 未安装

**解决方法**:
```bash
pip install openai
```

### 问题 4: pip 安装失败

**原因**: pip 版本过旧或网络问题

**解决方法**:
```bash
# 更新 pip
python -m pip install --upgrade pip

# 使用国内镜像源（如果网络慢）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 问题 5: API Key 相关错误

**症状**: AI 解析时提示 API Key 错误

**解决方法**:
1. 检查 API Key 是否正确
2. 确认已设置环境变量
3. 检查网络连接

**获取 DeepSeek API Key**:
1. 访问 https://platform.deepseek.com/
2. 注册账号
3. 在控制台创建 API Key

## 📋 完整安装检查清单

运行以下命令逐一检查：

```bash
# 1. 检查 Python 版本
python --version
# 应该显示 Python 3.8 或更高版本

# 2. 检查 pip
pip --version

# 3. 检查 PyPDF2
python -c "import PyPDF2; print('PyPDF2:', PyPDF2.__version__)"

# 4. 检查 OpenAI SDK
python -c "import openai; print('OpenAI SDK 已安装')"

# 5. 检查 Tkinter
python -c "import tkinter; print('Tkinter 可用')"

# 6. 检查 Flask（网页版）
python -c "import flask; print('Flask:', flask.__version__)"

# 7. 检查 Flask-CORS（网页版）
python -c "from flask_cors import CORS; print('Flask-CORS 已安装')"
```

**所有检查都通过后，您就可以使用程序了！** ✅

## 🔧 开发环境设置（可选）

如果您想修改代码或进行开发，建议安装以下工具：

### 虚拟环境

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt
```

### 代码编辑器

推荐使用以下编辑器：
- **VS Code** - 已包含配置文件 `.vscode/settings.json`
- **PyCharm** - 功能强大的 Python IDE
- **Sublime Text** - 轻量级编辑器

### VS Code 扩展（推荐）

如果使用 VS Code，安装以下扩展：
- Python (Microsoft)
- Pylance
- Python Indent

## 📦 离线安装

如果在没有网络的环境中安装：

### 1. 下载依赖包

在有网络的机器上：
```bash
pip download -r requirements.txt -d packages/
```

### 2. 离线安装

在目标机器上：
```bash
pip install --no-index --find-links=packages/ -r requirements.txt
```

## 🌐 网络配置

### 配置代理（如果需要）

```bash
# 设置 HTTP 代理
export HTTP_PROXY="http://proxy.example.com:8080"
export HTTPS_PROXY="http://proxy.example.com:8080"

# 或者在 pip 中使用代理
pip install -r requirements.txt --proxy=http://proxy.example.com:8080
```

### 使用国内镜像源

**清华大学镜像：**
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**阿里云镜像：**
```bash
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
```

## 📊 系统资源要求

| 资源 | 最低要求 | 推荐配置 |
|------|---------|---------|
| CPU | 1 核心 | 2+ 核心 |
| 内存 | 512 MB | 2 GB+ |
| 磁盘 | 100 MB | 500 MB+ |
| 网络 | AI解析需要 | 稳定连接 |

## 🎓 下一步

安装完成后，请阅读：

1. **README_Tkinter版本.md** - 详细使用说明
2. **项目总结.md** - 项目概览
3. **问题排查指南.md** - 遇到问题时查看

---

**安装遇到问题？** 请查看 `问题排查指南.md` 或在日志窗口查看详细错误信息。

**祝您使用愉快！** 🎉
