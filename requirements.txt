# PDF试卷模拟考试器 - 轻量版
# 纯HTML方案，无需安装任何Python依赖
# 直接打开 Exam_Lite.html 即可使用

# Web 服务器
Flask>=3.0.0
flask-cors>=4.0.0

# 配置文件支持
python-dotenv>=1.0.0

# 标准库（无需安装）
# json, re, threading, datetime, tempfile, pathlib
